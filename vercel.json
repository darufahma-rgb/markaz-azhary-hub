{
  "version": 2,
  "public": true,
  "installCommand": "cd artifacts/markaz-rabithah && pnpm install --ignore-workspace --no-frozen-lockfile",
  "buildCommand": "cd artifacts/markaz-rabithah && pnpm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
